import { withInstall } from '../../utils';

import intervalTime from './src/intervalTime';

export const IntervalTime = withInstall(intervalTime);
export default IntervalTime;

export * from './src';
